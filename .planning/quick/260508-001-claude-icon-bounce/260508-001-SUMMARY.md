---
id: 260508-001
slug: claude-icon-bounce
status: complete
date: 2026-05-08
---

# Quick Task 260508-001 — Summary

## What changed
- `App/Assets.xcassets/Contents.json` (신규) — asset catalog 루트 매니페스트
- `App/Assets.xcassets/ClaudeCodeIcon.imageset/Contents.json` (신규) — SVG 벡터 보존 옵션
- `App/Assets.xcassets/ClaudeCodeIcon.imageset/claudecode-color.svg` (신규) — Claude Code 공식 픽셀 글리프 (#D97757)
- `App/WidgetIconView.swift` — `bell.badge.fill` SF Symbol 자리표시자를 `Image("ClaudeCodeIcon")`로 교체. 5pt 진폭 / 0.45s easeInOut / autoreverses / repeatForever 위아래 바운스 추가. Reduce Motion 환경 변수 가드 포함. 헤더 주석 갱신.

## Verification
- `xcodegen generate` — 성공, 새 asset catalog 자동 인식
- `xcodebuild … build` — `BUILD SUCCEEDED`, `Assets.car`이 `.app/Contents/Resources`에 포함됨
- `xcodebuild … test` — 79/79 통과

## Notes
- 바운스는 ZStack 내부 Image 한 곳에만 적용되어 +N 배지(topTrailing 앵커, -4/-4 overhang)는 정지 상태 유지.
- Asset에 `preserves-vector-representation: true` 설정 → 모든 스케일에서 깔끔하게 렌더.
- Phase 6의 "self-made glyph" 교체 작업은 본 변경과 독립적으로 수행 가능 (`Image("ClaudeCodeIcon")` 호출부 1줄만 바뀜).
