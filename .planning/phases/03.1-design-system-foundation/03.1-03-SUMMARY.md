---
phase: 03.1-design-system-foundation
plan: 03
subsystem: design-system
tags: [refactor, design-tokens, popover, ui]
requires:
  - "App/DesignTokens.swift::GeometryTokens.popoverWidth"
  - "App/DesignTokens.swift::GeometryTokens.rowMinHeight"
  - "App/DesignTokens.swift::GeometryTokens.popoverMaxVisibleRows"
provides:
  - "PopoverContentView container layout consuming GeometryTokens (SC#1)"
affects:
  - "Plan 05 (visual-fidelity-checkpoint) — must visually verify popover unchanged"
tech-stack:
  added: []
  patterns:
    - "Token-resolved geometry literals via same-module enum reference"
key-files:
  created: []
  modified:
    - "App/PopoverContentView.swift"
decisions:
  - "F-1 honoured: GeometryTokens.popoverWidth = 280 is the source of truth (SPEC.md §3 already reconciled to 280 in commit a8f9a96)"
  - "F-2 retained literals (11pt button font, 12pt horizontal padding, 8pt top padding, 모두 지우기 copy, .thinMaterial background) intentionally NOT tokenised — out of scope per plan"
metrics:
  duration: ~3 min
  completed: 2026-05-09
  tasks_completed: 1
  files_modified: 1
  commits: 1
---

# Phase 03.1 Plan 03: Popover Content Refactor Summary

Refactored `PopoverContentView` geometry literals (lines 86 & 88) to consume `GeometryTokens.popoverWidth`, `GeometryTokens.rowMinHeight`, and `GeometryTokens.popoverMaxVisibleRows` — behaviour-preserving (288pt max scroll height, 280pt fixed width identical to prior code).

## Diff Summary

3 changed lines in `App/PopoverContentView.swift`:

| Line (post-edit) | Change | Before | After |
|---|---|---|---|
| 5 (added) | F-1 traceability comment | — | `// Phase 03.1 — geometry literals consume GeometryTokens (F-1: token = 280, code SoT, not SPEC's 270).` |
| 87 | `.frame(maxHeight:)` | `36 * 8   // UI-SPEC: max 8 visible rows` | `GeometryTokens.rowMinHeight * CGFloat(GeometryTokens.popoverMaxVisibleRows)` |
| 89 | `.frame(width:)` | `280` | `GeometryTokens.popoverWidth` |

`git diff --stat`: `1 file changed, 3 insertions(+), 2 deletions(-)` — well under the plan's 5-line over-edit threshold.

Commit: `9d02723` — `refactor(03.1-03): consume GeometryTokens in PopoverContentView`.

## F-1 Confirmation

`GeometryTokens.popoverWidth = 280` is the code source of truth. SPEC.md §3 was already reconciled from 270 → 280 in upstream commit `a8f9a96` (independent of this plan), so token + SPEC + runtime literal now all agree on 280pt. No SPEC drift remains for this dimension.

## Token Consumption Count

`grep -E 'GeometryTokens\.' App/PopoverContentView.swift | wc -l` → **2 lines** (line 87 references both `.rowMinHeight` and `.popoverMaxVisibleRows`; line 89 references `.popoverWidth`). Total distinct token references: **3** — exactly what the plan required.

Verification grep results:
- `GeometryTokens.popoverWidth`: 1 occurrence
- `GeometryTokens.rowMinHeight`: 1 occurrence
- `GeometryTokens.popoverMaxVisibleRows`: 1 occurrence
- Old `frame(width: 280)`: 0 occurrences (removed)
- Old `maxHeight: 36 * 8`: 0 occurrences (removed)
- `.thinMaterial` (F-2 retained): 1 occurrence (preserved)
- `PopoverContentRules` (untouched namespace): 4 occurrences (preserved)
- "모두 지우기" (F-2 retained copy): 2 occurrences (preserved)

## F-2 Retained Literals (this file, intentionally out of scope)

Per Finding F-2 — these survive the refactor unchanged because they lack a SPEC §3/§4 row binding to this view's layout, or they are non-numeric (copy/material):

| Literal | Location | Reason retained |
|---|---|---|
| `12pt` horizontal padding | line 70 (`.padding(.horizontal, 12)` on the "모두 지우기" HStack) | F-2: no SPEC §3 row binds chrome padding of the clear-all button block |
| `8pt` top padding | line 71 (`.padding(.top, 8)`) | F-2: same — chrome padding without SPEC binding |
| `11pt` button font | line 66 (`.font(.system(size: 11))`) | F-2: typography token namespace not in scope for Phase 03.1 |
| `"모두 지우기"` copy + `"모든 알림 지우기"` accessibility label | lines 64, 68 | F-2: non-numeric / non-color, copy not subject to design tokens |
| `.thinMaterial` background | line 90 | F-2: non-numeric material token not in this plan's scope |
| `Color(NSColor.secondaryLabelColor)` | line 67 | F-2: system semantic color, not a custom hex |

These are documented for future plans that may address typography / chrome / material tokens.

## Verification Results

- Token consumption greps: all PASS (3 distinct tokens, 0 stale literals)
- F-2 sentinels (`.thinMaterial`, `PopoverContentRules`, `모두 지우기`) preserved
- `xcodebuild build -scheme ClaudeAlertBot -destination 'platform=macOS'` → `** BUILD SUCCEEDED **`
- `xcodebuild test -only-testing:ClaudeAlertBotTests/PopoverContentTests` → `Executed 5 tests, with 0 failures` — `PopoverContentRules` namespace logic untouched
- Diff scope: 3 lines changed (well under the 5-line over-edit threshold)
- Visual parity: 36*8 = 288 ≡ `36.0 * 8.0` = 288.0; 280 ≡ 280.0 — identical layout output

## Deviations from Plan

None — plan executed exactly as written. F-1 token value (280) and the no-tokenise-this-time decisions for F-2 literals were both honoured. No drive-by edits.

## Self-Check: PASSED

- File edit verified at `App/PopoverContentView.swift` (3 lines changed; greps confirmed token presence + literal removal)
- Commit `9d02723` exists in `git log` on `worktree-agent-afe6e67f28eab125f`
- Build + tests green
- SUMMARY.md created and about to be committed
