---
phase: 03.1-design-system-foundation
plan: 04
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - App/PopoverRowView.swift
autonomous: true
requirements: []
must_haves:
  truths:
    - "PopoverRowView's row geometry literals (36pt min height, 12pt horizontal padding, 8pt vertical padding) resolve through GeometryTokens (SC#1)"
    - "Row layout, hover state, and Phase 3 D3-11 missing animation behaviour are visually indistinguishable from before (SC#4)"
  artifacts:
    - path: "App/PopoverRowView.swift"
      provides: "Row view consuming GeometryTokens; non-SPEC literals retained per F-2"
      contains: "GeometryTokens.rowMinHeight"
  key_links:
    - from: "App/PopoverRowView.swift"
      to: "App/DesignTokens.swift"
      via: "same-module reference"
      pattern: "GeometryTokens"
---

<objective>
Refactor `App/PopoverRowView.swift` to consume `GeometryTokens.rowMinHeight` (36), `GeometryTokens.rowVerticalPadding` (8), and `GeometryTokens.rowHorizontalPadding` (12) instead of inline literals on lines 70-72. **NO behavioral change**: row layout, hover affordance, and the Phase 3 D3-11 missing animation (도리도리 + collapse) all behave identically.

Per Finding F-2 (recorded in Plan 01): hover color (`Color(NSColor.controlAccentColor).opacity(0.12)`), hover animation (`easeInOut(duration: 0.12)`), Phase 3 missing-animation literals (`0.15s × 2`, `12°`, `0.4s` collapse, `0.2s` reduce-motion fallback), and typography literals (`13pt` row body, `11pt` time suffix / `?` indicator) all stay inline with the existing comments. They have no SPEC §4 binding (hover anim) or have non-trivial Phase 3 contracts (D3-11 motion sequence) we are explicitly out-of-scope to disturb. The reduce-motion gate inside `runMissingAnimation` likewise stays — it is Phase 3 D3-11 contract, not bounce-style infinite loop, and SC#3's "uniform" requirement is delivered by Plan 02 for the bounce path.

Purpose: SC#1 + SC#4 for the row component.
Output: Minimum-diff edit to `PopoverRowView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@SPEC.md
@CLAUDE.md
@App/PopoverRowView.swift
@App/DesignTokens.swift
@.planning/phases/03.1-design-system-foundation/03.1-01-SUMMARY.md
</context>

<interfaces>
<!-- Frozen from Plan 01. -->
From `App/DesignTokens.swift`:
```swift
enum GeometryTokens {
    static let rowMinHeight: CGFloat = 36
    static let rowVerticalPadding: CGFloat = 8
    static let rowHorizontalPadding: CGFloat = 12
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace row geometry literals in PopoverRowView with GeometryTokens</name>
  <files>App/PopoverRowView.swift</files>
  <read_first>
    - App/PopoverRowView.swift (full pass — note that lines 95-125 are the Phase 3 D3-11 missing animation; do not touch)
    - App/DesignTokens.swift
    - SPEC.md §3 §"Geometry"
    - .planning/phases/03.1-design-system-foundation/03.1-01-SUMMARY.md (F-2 retained literals — explicit list of what stays inline)
  </read_first>
  <action>
    Apply the minimum-diff patch below. Per CLAUDE.md "No Over-Editing": preserve every line not explicitly listed. The `runMissingAnimation` body (lines 96-125) MUST remain byte-identical — it is Phase 3 D3-11 locked contract (도리도리 ±12° → collapse + fade).

    **Edit 1 — line 70** (currently `.padding(.vertical, 8)`):
    Replace with:
    ```swift
    .padding(.vertical, GeometryTokens.rowVerticalPadding)
    ```

    **Edit 2 — line 71** (currently `.padding(.horizontal, 12)`):
    Replace with:
    ```swift
    .padding(.horizontal, GeometryTokens.rowHorizontalPadding)
    ```

    **Edit 3 — line 72** (currently `.frame(minHeight: collapsed ? 0 : 36)`):
    Replace with:
    ```swift
    .frame(minHeight: collapsed ? 0 : GeometryTokens.rowMinHeight)
    ```
    Behaviour preservation: when `collapsed == true`, minHeight is 0 (collapse animation); when `collapsed == false`, minHeight is 36pt — identical to before. The Phase 3 D3-11 collapse animation in `runMissingAnimation` continues to drive this property change unchanged.

    **Edit 4 — header comment** add a single line above line 14 (`import SwiftUI`):
    ```swift
    // Phase 03.1: row geometry literals (36/12/8) resolve through GeometryTokens (SC#1).
    // Non-SPEC literals retained per Finding F-2: hover color/anim (controlAccentColor + 0.12s),
    // Phase 3 D3-11 missing-animation timings (0.15s × 2 + 0.4s collapse + 0.2s reduce-motion fallback),
    // typography (13pt body, 11pt secondary). See 03.1-01-SUMMARY.md.
    ```

    **DO NOT** change:
    - `import` block
    - `RowState` enum (lines 18-22) — Phase 3 D3-11 contract
    - `PopoverRowView` struct declaration, properties, `@State` lines, `body` opener
    - The Button label HStack (lines 47-68) — typography literals (13pt, 11pt) retained per F-2; opacity, rotationEffect, lineLimit, truncationMode all preserved
    - Lines 74-77: `.background(...)`, hover color `Color(NSColor.controlAccentColor).opacity(0.12)`, hover animation `easeInOut(duration: 0.12)` — F-2 retained (not in SPEC §3 row-hover; SPEC uses accent-tinted, code uses system accent — keep code behaviour)
    - Lines 78-91: `.clipped()`, `.contentShape`, `.buttonStyle`, `.disabled`, `.onHover`, `.onChange`, `.accessibilityLabel`
    - Lines 96-125: entire `runMissingAnimation()` function — Phase 3 D3-11 locked contract. The `0.15`, `12.0`, `-12.0`, `0.4`, `0.2` literals stay inline. The `NSWorkspace.shared.accessibilityDisplayShouldReduceMotion` reads stays — it is the AppKit-side reduce-motion source for non-SwiftUI-Environment code; SC#3's uniform-gate requirement is delivered through `MotionTokens.bounceAnimation` (Plan 02), not through retroactively rewiring Phase 3's contract.

    Final diff scope: 3 line replacements + 1 multiline header comment. Git diff `+`/`-` line count should stay around 8-10. If more than ~12, STOP.
  </action>
  <verify>
    <automated>set -e; cd /Users/choijihye/Study/source/claude_alert_bot &amp;&amp; grep -c 'GeometryTokens.rowMinHeight' App/PopoverRowView.swift &amp;&amp; grep -c 'GeometryTokens.rowVerticalPadding' App/PopoverRowView.swift &amp;&amp; grep -c 'GeometryTokens.rowHorizontalPadding' App/PopoverRowView.swift &amp;&amp; ! grep -E 'padding\(\.vertical,\s*8\)' App/PopoverRowView.swift &amp;&amp; ! grep -E 'padding\(\.horizontal,\s*12\)' App/PopoverRowView.swift &amp;&amp; ! grep -E 'minHeight:\s*collapsed\s*\?\s*0\s*:\s*36\b' App/PopoverRowView.swift &amp;&amp; grep -c 'controlAccentColor' App/PopoverRowView.swift &amp;&amp; grep -c 'runMissingAnimation' App/PopoverRowView.swift &amp;&amp; grep -c 'rotation = 12' App/PopoverRowView.swift &amp;&amp; grep -c 'F-2' App/PopoverRowView.swift &amp;&amp; xcodebuild build -scheme ClaudeAlertBot -destination 'platform=macOS' 2>&amp;1 | tail -5 | grep -c 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>
    - `grep -c 'GeometryTokens.rowMinHeight' App/PopoverRowView.swift` ≥1.
    - `grep -c 'GeometryTokens.rowVerticalPadding' App/PopoverRowView.swift` ≥1.
    - `grep -c 'GeometryTokens.rowHorizontalPadding' App/PopoverRowView.swift` ≥1.
    - Old runtime literals removed: 3 negative greps in <verify> all return 0 lines.
    - F-2 retained literals confirmed: `controlAccentColor` ≥1, `runMissingAnimation` ≥2 (declaration + invocation), `rotation = 12` ≥1 (D3-11 도리도리 contract preserved).
    - F-2 annotation present: `grep -c 'F-2' App/PopoverRowView.swift` ≥1.
    - Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. Token consumption: `grep -E 'GeometryTokens\.' App/PopoverRowView.swift | wc -l` ≥3.
2. Old literals removed from runtime code: 3 negative greps each return 0 lines.
3. Phase 3 D3-11 contract intact (no drive-by changes to missing animation):
   - `grep -c 'runMissingAnimation' App/PopoverRowView.swift` ≥2 (decl + call)
   - `grep -c 'rotation = 12' App/PopoverRowView.swift` ≥1
   - `grep -c 'rotation = -12' App/PopoverRowView.swift` ≥1
   - `grep -c 'NSWorkspace.shared.accessibilityDisplayShouldReduceMotion' App/PopoverRowView.swift` ≥1
4. Build succeeds.
</verification>

<success_criteria>
- Row geometry (36pt min, 12pt horiz, 8pt vert) resolves through GeometryTokens.
- Phase 3 D3-11 contract preserved byte-for-byte in `runMissingAnimation`.
- Hover affordance untouched (F-2).
- Visual fidelity preserved (Plan 05 manual checkpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-design-system-foundation/03.1-04-SUMMARY.md` covering:
- Diff summary (3 line replacements + header comment)
- F-2 retained literal list for this file: hover color/anim, D3-11 missing animation timings, typography
- Phase 3 D3-11 contract preservation evidence (line numbers of 도리도리 sequence still byte-identical)
- Token consumption count
</output>
