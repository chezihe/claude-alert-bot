---
phase: 03.1-design-system-foundation
plan: 04
subsystem: design-system
tags: [design-tokens, refactor, swiftui, behaviour-preserving]
requires:
  - "03.1-01 — App/DesignTokens.swift (GeometryTokens namespace, frozen Wave 2 surface)"
provides:
  - "App/PopoverRowView.swift consuming GeometryTokens.rowMinHeight / rowVerticalPadding / rowHorizontalPadding"
  - "F-2 retained literals annotated inline (header comment)"
affects:
  - "03.1-05 — Wave 3 manual visual checkpoint can now verify token-driven row layout"
tech-stack:
  added: []
  patterns:
    - "consume GeometryTokens for SPEC §3 row literals; keep non-SPEC literals inline per F-2"
key-files:
  created: []
  modified:
    - "App/PopoverRowView.swift"
decisions:
  - "Minimum-diff edit: 3 line replacements + 4-line header comment (10 lines net diff)"
  - "runMissingAnimation body byte-identical — Phase 3 D3-11 contract is locked"
  - "F-2 retained literals (hover color/anim, D3-11 timings, typography) stay inline with explicit header comment"
metrics:
  duration: ~3 min
  completed: 2026-05-09
---

# Phase 03.1 Plan 04: PopoverRowView Token Refactor Summary

**One-liner:** `App/PopoverRowView.swift` row geometry literals (8 / 12 / 36) now resolve through `GeometryTokens.rowVerticalPadding` / `rowHorizontalPadding` / `rowMinHeight`; behaviour-preserving — Phase 3 D3-11 missing-animation contract intact byte-for-byte.

## Objective Achieved

- SC#1 (row component): row geometry resolves through `GeometryTokens` — 3 token call-sites in `PopoverRowView.swift` (`rowVerticalPadding`, `rowHorizontalPadding`, `rowMinHeight`).
- SC#4: row layout, hover affordance, and Phase 3 D3-11 missing animation behave identically — non-token literals untouched.
- Per CLAUDE.md "No Over-Editing": every line not explicitly targeted is preserved. Diff is 7 insertions + 3 deletions.

## Diff Summary

| Edit | Location | Change |
|---|---|---|
| 1 | line 70 (was) | `.padding(.vertical, 8)` → `.padding(.vertical, GeometryTokens.rowVerticalPadding)` |
| 2 | line 71 (was) | `.padding(.horizontal, 12)` → `.padding(.horizontal, GeometryTokens.rowHorizontalPadding)` |
| 3 | line 72 (was) | `.frame(minHeight: collapsed ? 0 : 36)` → `.frame(minHeight: collapsed ? 0 : GeometryTokens.rowMinHeight)` |
| 4 | header (above `import SwiftUI`) | Added 4-line F-2 annotation block |

`git diff --stat`: `1 file changed, 7 insertions(+), 3 deletions(-)`.

## F-2 Retained Literals in This File (annotated in header comment)

These remain as inline literals — explicitly out-of-scope per Finding F-2 (recorded in `03.1-01-SUMMARY.md`):

- **Hover affordance** — `Color(NSColor.controlAccentColor).opacity(0.12)` + `.animation(.easeInOut(duration: 0.12), value: isHovered)`. SPEC §3 row-hover specifies `rgba(217,119,87,0.13)` (accent-tinted), but the existing code uses system-accent. SC#4 says behaviour-preserving — keep code SoT. Tokenisation deferred to Phase 4 polish.
- **Phase 3 D3-11 missing-animation timings** — `0.15s × 2` (rotation halves), `12°` / `−12°` (도리도리 amplitude), `0.4s` collapse + fade, `0.2s` reduce-motion fallback. Phase 3 contract; SPEC §4 has no entry. SC#3's uniform reduce-motion gate is delivered for the bounce path (Plan 02), not by retroactively rewiring D3-11.
- **Typography** — `13pt` row body (`session.projectName`), `11pt` time suffix (`·`), `11pt` orphan `?` indicator. SPEC §3 typography table doesn't bind these to row-view structure; treated as Phase 4+ polish.

## Phase 3 D3-11 Contract Preservation Evidence

`runMissingAnimation()` (lines 100-129 post-edit; was lines 96-125) is byte-identical to pre-refactor. The 도리도리 sequence is preserved verbatim:

| Element | Line (post-edit) | Verified |
|---|---|---|
| `let reduceMotion = NSWorkspace.shared.accessibilityDisplayShouldReduceMotion` | 101 | grep ≥1 |
| Reduce-motion fallback `easeInOut(duration: 0.2)` + immediate `collapsed = true` / `faded = true` | 102-110 | grep ≥1 |
| Phase 1 도리도리 — `withAnimation(.easeInOut(duration: 0.15)) { rotation = 12 }` | 114 | grep `rotation = 12` ≥1 |
| Phase 1 — `withAnimation(.easeInOut(duration: 0.15)) { rotation = -12 }` | 116 | grep `rotation = -12` ≥1 |
| Settle to 0 then chain | 119 | preserved |
| Phase 2 collapse + fade — `withAnimation(.easeInOut(duration: 0.4), completionCriteria: .logicallyComplete)` → `onMissingComplete()` | 121-126 | preserved |

The `.onChange(of: state)` watcher and `.disabled(state != .normal)` JUMP-05 self-debounce are also untouched.

## Token Consumption Count

`grep -E 'GeometryTokens\.' App/PopoverRowView.swift | wc -l` → **3** (target ≥3).

## Verification Results

| Check | Result |
|---|---|
| `grep -c 'GeometryTokens.rowMinHeight'` | 1 (≥1) |
| `grep -c 'GeometryTokens.rowVerticalPadding'` | 1 (≥1) |
| `grep -c 'GeometryTokens.rowHorizontalPadding'` | 1 (≥1) |
| `grep -E 'padding\(\.vertical,\s*8\)'` (negative) | 0 lines |
| `grep -E 'padding\(\.horizontal,\s*12\)'` (negative) | 0 lines |
| `grep -E 'minHeight:\s*collapsed\s*\?\s*0\s*:\s*36\b'` (negative) | 0 lines |
| `grep -c 'controlAccentColor'` | 2 (F-2 hover affordance retained) |
| `grep -c 'runMissingAnimation'` | 2 (decl + invocation, D3-11 preserved) |
| `grep -c 'rotation = 12'` | 1 (도리도리 +12°) |
| `grep -c 'rotation = -12'` | 1 (도리도리 −12°) |
| `grep -c 'NSWorkspace.shared.accessibilityDisplayShouldReduceMotion'` | 1 (D3-11 reduce-motion gate) |
| `grep -c 'F-2'` | 1 (header annotation) |
| `xcodebuild build -scheme ClaudeAlertBot -destination 'platform=macOS'` | **BUILD SUCCEEDED** |

Pre-existing unrelated warnings observed during build (Sendable conformance on `NSAppleScript` / `WakeObserver` / `FloatingWidgetWindowController`; "Run script build phase will run during every build" on two embed-script phases). Out of scope for this plan — neither caused by this edit nor in target file.

## Deviations from Plan

None — plan executed exactly as written. Diff scope landed at 7+/3- (within the ~12 line budget).

## Commits

| Task | Hash | Message |
|---|---|---|
| 1 | `aa63e9c` | `refactor(03.1-04): consume GeometryTokens in PopoverRowView (SC#1)` |

## Self-Check: PASSED

- App/PopoverRowView.swift: FOUND (modified, all 3 token call-sites present)
- Commit aa63e9c: FOUND on `worktree-agent-aac539352a5ebadbd`
- Build: SUCCEEDED
- Phase 3 D3-11 contract: byte-identical (5 grep checks passed)
- F-2 retained literals: confirmed inline + header annotation present
