---
id: 260508-002
slug: popover-unavailable-state
status: complete
date: 2026-05-08
---

# Quick Task 260508-002 — Summary

## What changed
- `App/PopoverContentView.swift` — `PopoverContentRules.isUnavailable(sessionID:in:)` 순수 함수 + `unavailableLabelText = "Session unavailable"` 상수. `PopoverContentView`에 `var unavailableSessionIDs: Set<String> = []` 디폴트 파라미터 추가, `ForEach`에서 row별로 `isAvailable` 도출.
- `App/PopoverRowView.swift` — `var isAvailable: Bool = true` 디폴트 파라미터 추가. `!isAvailable`일 때 트레일링 영역(time + orphan)을 `Session unavailable` 보조 라벨로 교체, 행 opacity 0.5, accessibilityLabel 분기.
- `ClaudeAlertBotTests/PopoverContentTests.swift` — 신규 테스트 3개:
  - `test_isUnavailable_membershipCheck`
  - `test_isUnavailable_emptySet_neverUnavailable`
  - `test_unavailableLabelText_minimalEnglishCopy_locked`

## Verification
- `xcodebuild … test` — 82/82 통과 (기존 79 + 신규 3, 회귀 0).
- 디폴트 인자 덕분에 `WidgetPopoverController.showPopover()` 호출부는 컴파일 무변경.

## Phase 3 wire-in 지점
ITermBridge가 점프 실패 시:
1. `WidgetPopoverController` 안에 `private var unavailableSessionIDs: Set<String> = []` 보유
2. ITermBridge.jump(...) 결과가 lookup miss → `unavailableSessionIDs.insert(sessionID)` + popover 재구성
3. `PopoverContentView(queue: ..., onRowClick: ..., onClearAll: ..., unavailableSessionIDs: unavailableSessionIDs)` 호출 변경

## Tone lockdown (메모리 등록)
- 위젯/팝오버 카피는 **미니멀 영어**, "session" 용어 통일. 컬러 dot / 이모지 / ✨ 등 장식 요소 지양.
- `feedback_minimal_ui_copy.md`에 영구 기록 — 향후 디자인 제안 시 lead with English minimal candidates.
