---
phase: 03.1-design-system-foundation
plan: 05
type: execute
wave: 3
depends_on: ["03.1-02", "03.1-03", "03.1-04"]
files_modified:
  - .planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md
  - .planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md
  - .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Phase 2 verifier (scripts/verify-phase-2.sh) and full XCTest target stay green after the refactor (SC#4)"
    - "Existing widget + popover are visually indistinguishable from before the refactor — confirmed by manual checkpoint with annotated screenshot slots (SC#4)"
    - "Token file is consumable by Phase 4 multi-session views without modification — Phase 4 token name list is recorded in 03.1-SUMMARY.md (SC#5)"
  artifacts:
    - path: ".planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md"
      provides: "Pre/post-refactor screenshot annotation + side-by-side verdict checklist"
    - path: ".planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md"
      provides: "Phase-gate sign-off with all 5 SCs verified"
    - path: ".planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md"
      provides: "Phase summary consolidating F-1/F-2/F-3 findings + Phase 4 readiness contract"
  key_links:
    - from: "03.1-SUMMARY.md → Phase 4 readiness section"
      to: "App/DesignTokens.swift exported symbols"
      via: "explicit token-name list Phase 4 plans must reference"
      pattern: "ColorTokens.|GeometryTokens.|MotionTokens."
---

<objective>
Verify SC#4 (visual indistinguishability + verifier green) and SC#5 (Phase 4 readiness) end-to-end. This plan is intentionally not autonomous — Task 2 is a manual human-verify checkpoint where the user compares pre/post-refactor screenshots of the live widget and popover.

Purpose: SC#4, SC#5, phase closure (`phase_gate: green`).
Output: 3 documentation artifacts (MANUAL-CHECKPOINT.md, VERIFICATION.md, SUMMARY.md) and a phase_gate verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@SPEC.md
@CLAUDE.md
@App/DesignTokens.swift
@App/WidgetIconView.swift
@App/PopoverContentView.swift
@App/PopoverRowView.swift
@scripts/verify-phase-2.sh
@.planning/phases/03.1-design-system-foundation/03.1-01-SUMMARY.md
@.planning/phases/03.1-design-system-foundation/03.1-02-SUMMARY.md
@.planning/phases/03.1-design-system-foundation/03.1-03-SUMMARY.md
@.planning/phases/03.1-design-system-foundation/03.1-04-SUMMARY.md
@.planning/phases/02-alert-loop/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 2 verifier + full XCTest target; capture before/after screenshot helper</name>
  <files>.planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md</files>
  <read_first>
    - scripts/verify-phase-2.sh (sanity-check what rows execute)
    - .planning/phases/02-alert-loop/02-VERIFICATION.md (template for sign-off shape)
  </read_first>
  <action>
    1. Build and ad-hoc-sign the Release artifact:
       ```sh
       bash scripts/build.sh
       ```
       (Same script Phase 1 / Phase 2 / Phase 3 use; produces a launchable .app under `build/Release/`.)

    2. Run the Phase 2 verifier non-interactively and capture the result:
       ```sh
       VERIFY_NONINTERACTIVE=1 bash scripts/verify-phase-2.sh > /tmp/03.1-verify-phase-2.log 2>&1
       echo "EXIT=$?"
       ```
       Expected: exit 0 with the same PASS/FAIL/SKIP profile as Phase 2 closure (23 PASS / 1 FAIL\* / 2 SKIP per STATE.md, where the single FAIL is the documented `2-11-02` cab-test artifact). If counts differ, STOP and surface the diff.

    3. Run the full XCTest target:
       ```sh
       xcodebuild test -scheme ClaudeAlertBot -destination 'platform=macOS' 2>&1 | tail -40 > /tmp/03.1-xctest.log
       grep -c 'Test Suite .* passed' /tmp/03.1-xctest.log
       ```
       Expected: ≥1 "passed" line (the root suite). Test count was 79 at Phase 2 close; should now be 79 + 13 (DesignTokensTests) = 92 minimum. If any test fails, STOP.

    4. Write `.planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md` from this template (annotation slots are filled in Task 2 by the human):

       ```markdown
       # 03.1 Manual Checkpoint — Visual Fidelity (SC#4)

       **Goal:** Confirm the widget and popover are visually indistinguishable before vs after the design-token refactor.

       ## Automated pre-checks (Task 1 — completed)

       - [x] `scripts/verify-phase-2.sh` executed; full log at `/tmp/03.1-verify-phase-2.log`
         - PASS count: __ (expected: same as Phase 2 closure, see STATE.md)
         - FAIL count: __ (expected: ≤1 documented `2-11-02` artifact)
         - SKIP count: __
       - [x] Full XCTest target executed; log at `/tmp/03.1-xctest.log`
         - Total test count: __ (expected: Phase 2 closure 79 + DesignTokensTests 13 = ≥92)
         - All passed: __ (expected: yes)

       ## Manual visual checks (human, side-by-side)

       Capture pre-refactor screenshots BEFORE running this plan against your local checkout. The recommended commit pin for "before" is `git rev-parse HEAD~5` (or whichever commit precedes Plan 01). After Plan 04 lands, capture "after" screenshots.

       ### Floating widget (WidgetIconView)

       - [ ] **W-1: Idle bounce — 30s observation.** Vertical travel ≈ 5pt; cycle ≈ 0.45s up + 0.45s down (eye-balled, not measured). Cadence indistinguishable from before.
         - Before screenshot: `[paste path or attachment]`
         - After screenshot: `[paste path or attachment]`
         - Verdict: __

       - [ ] **W-2: Reduce-motion toggle.** System Settings → Accessibility → Display → "Reduce motion" ON: bounce stops within ~1 cycle. OFF: bounce resumes.
         - Behaviour matches pre-refactor: __

       - [ ] **W-3: +N badge alignment.** With pendingCount ≥ 2, the red badge sits at top-trailing with the same `-4/-4` overhang and 16pt diameter. (F-2 inline literals retained.)
         - Verdict: __

       ### Popover (PopoverContentView + PopoverRowView)

       - [ ] **P-1: Width.** Popover is 280pt wide (post-refactor token); side-by-side with pre-refactor screenshot, no horizontal shift.
         - Verdict: __

       - [ ] **P-2: Row height.** Each row is 36pt min; row count of 8 fills the popover ScrollView region without an inner scrollbar; row count ≥ 9 introduces the scrollbar.
         - Verdict: __

       - [ ] **P-3: Row hover.** Hovering a row tints it with `controlAccentColor.opacity(0.12)` (system-blue tint), same as before. (F-2 retained.)
         - Verdict: __

       - [ ] **P-4: Phase 3 D3-11 missing animation.** Click a row whose iTerm2 tab no longer exists → 도리도리 ±12° once, then collapse + fade over ~0.4s. Same shape and timing as before.
         - Verdict: __

       - [ ] **P-5: "모두 지우기" button.** Visible only when ≥2 rows. Position top-trailing, 11pt secondary-label color. (F-2 retained.)
         - Verdict: __

       ## Findings to surface (will be consolidated into 03.1-SUMMARY.md by Task 3)

       - [ ] F-1 (popoverWidth = 280, not SPEC's 270) acknowledged
       - [ ] F-2 (non-SPEC inline literals retained) acknowledged
       - [ ] F-3 (`accent` token has zero call-sites in current code, defined for Phase 4) acknowledged

       ## Resume signal

       Type `approved` to proceed to phase closure (Task 3 will write 03.1-VERIFICATION.md and 03.1-SUMMARY.md).
       Or describe any visual regression observed — Task 3 will not run until each [ ] above is checked or annotated.
       ```

       Fill the `__` placeholders in "Automated pre-checks" with actual values from the logs in steps 2 and 3.
  </action>
  <verify>
    <automated>set -e; cd /Users/choijihye/Study/source/claude_alert_bot &amp;&amp; test -f .planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md &amp;&amp; grep -c 'W-1: Idle bounce' .planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md &amp;&amp; grep -c 'P-1: Width' .planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md &amp;&amp; grep -c 'P-4: Phase 3 D3-11 missing animation' .planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md &amp;&amp; VERIFY_NONINTERACTIVE=1 bash scripts/verify-phase-2.sh > /tmp/03.1-verify-phase-2.log 2>&amp;1; echo "verify-phase-2 exit=$?"; xcodebuild test -scheme ClaudeAlertBot -destination 'platform=macOS' 2>&amp;1 | tail -5 | grep -c 'Test Suite .* passed'</automated>
  </verify>
  <done>
    - `03.1-MANUAL-CHECKPOINT.md` exists with W-1..W-3, P-1..P-5 slots and F-1/F-2/F-3 acknowledgement boxes.
    - Phase 2 verifier completed; log at `/tmp/03.1-verify-phase-2.log`; PASS/FAIL/SKIP counts recorded in MANUAL-CHECKPOINT.md.
    - Full XCTest target completed; log at `/tmp/03.1-xctest.log`; total count recorded.
    - Counts match the expected profile from Phase 2 closure (no regressions).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Phase 2 verifier + full XCTest run completed and logged (Task 1).
    - Pre/post-refactor screenshot annotation document `03.1-MANUAL-CHECKPOINT.md` ready for human verification.
    - Token module + 3 view refactors landed (Plans 01-04).
  </what-built>
  <how-to-verify>
    Open `.planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md` and walk through it top to bottom.

    **Automated rows (Task 1 already filled):**
    1. Confirm verifier PASS/FAIL/SKIP counts match the Phase 2 closure profile (STATE.md "Plan 02-11 metrics" row: 23 PASS / 1 FAIL\* / 2 SKIP). If counts differ, surface the diff before approving.
    2. Confirm XCTest total ≥ 92 (Phase 2's 79 + DesignTokensTests 13).

    **Visual checks (you perform, side-by-side with pre-refactor screenshots):**

    Step A — produce pre-refactor screenshots (skip if you already have them from a baseline):
    ```sh
    git stash push --keep-index -- App/DesignTokens.swift App/WidgetIconView.swift App/PopoverContentView.swift App/PopoverRowView.swift
    bash scripts/build.sh
    open build/Release/ClaudeAlertBot.app
    # Trigger a Stop via the test injector; screenshot widget; hover the widget; screenshot the popover.
    # Save as ~/03.1-before-widget.png and ~/03.1-before-popover.png.
    git stash pop
    bash scripts/build.sh
    ```
    (If you already have a Phase 2 / Phase 3 baseline screenshot from a recent commit, skip the stash dance.)

    Step B — produce post-refactor screenshots:
    ```sh
    open build/Release/ClaudeAlertBot.app
    # Same injection + screenshot procedure. Save as ~/03.1-after-widget.png and ~/03.1-after-popover.png.
    ```

    Step C — open the four screenshots side-by-side (Preview's "Open in Preview" with multi-select shows them in one window). Walk W-1..W-3 and P-1..P-5 in MANUAL-CHECKPOINT.md.

    Step D — fill in each `Verdict: __` slot with `pass` or describe the regression. Tick each F-1/F-2/F-3 acknowledgement.

    **What "pass" means:**
    - W-1: Bounce period and amplitude match before within ~1 frame perceptually. Cadence indistinguishable.
    - W-2: Reduce-motion toggle still suppresses bounce.
    - P-1: Popover width unchanged (token = 280, was 280).
    - P-2: Row height + max-rows scroll boundary unchanged.
    - P-3: Hover tint identical (still `controlAccentColor.opacity(0.12)` — F-2 retained).
    - P-4: 도리도리 missing animation timing + arc unchanged (Phase 3 D3-11 contract preserved).
    - P-5: "모두 지우기" button styling and visibility rule unchanged.
  </how-to-verify>
  <resume-signal>
    Type `approved` to proceed to Task 3 (phase-gate sign-off).
    If any visual regression: describe it; Task 3 does not run; the phase remains open.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 03.1-VERIFICATION.md (phase_gate sign-off) + 03.1-SUMMARY.md (consolidated findings + Phase 4 readiness)</name>
  <files>.planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md, .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md (verdict slots — must be filled with `pass` or annotated regressions)
    - .planning/phases/02-alert-loop/02-VERIFICATION.md (template for sign-off prose shape)
    - .planning/phases/03-click-to-iterm2/03-VERIFICATION.md (template for SC-by-SC verdict block)
    - All four 03.1-NN-SUMMARY.md files
  </read_first>
  <action>
    Only run after the human approves Task 2.

    **A. Write `03.1-VERIFICATION.md`** with:
    - Header: phase, date 2026-05-09, gate verdict (`phase_gate: green` only if all 5 SCs pass)
    - SC#1 verdict: cite grep counts of `MotionTokens.` / `GeometryTokens.` / `ColorTokens.` references in the 3 view files; cite the negative greps showing old runtime literals removed
    - SC#2 verdict: cite DesignTokensTests pass count (13/13 expected)
    - SC#3 verdict: cite `MotionTokens.bounceAnimation(reduceMotion:)` factory consumption in WidgetIconView; explicitly note that PopoverRowView's `runMissingAnimation` retains `NSWorkspace.accessibilityDisplayShouldReduceMotion` per F-2 (Phase 3 D3-11 contract — uniform-gate requirement satisfied for the bounce path which is the only "scattered easeInOut(0.45)" target SC#3 mentions)
    - SC#4 verdict: cite the manual checkpoint approval + verify-phase-2 PASS/FAIL/SKIP counts + XCTest total count
    - SC#5 verdict: cite the Phase 4 readiness contract block in 03.1-SUMMARY.md (see step B)

    **B. Write `03.1-SUMMARY.md`** with:
    - Phase overview (1-2 paragraphs)
    - Plan-by-plan recap (01 → 05): what each delivered + commit hashes if available
    - **Findings consolidated:**
      - F-1: SPEC.md §3 says popover = 270pt, code/token = 280pt. Token follows code per Constraint D (visual indistinguishability). v1 follow-up: reconcile SPEC.md to 280 OR plan a v2 visual update to 270pt with a fresh manual-checkpoint pass.
      - F-2: Non-SPEC literals retained inline with annotations. Verbatim list:
        - WidgetIconView: 36/44pt glyph frame, 16pt × 16pt badge, `Color(NSColor.systemRed)` fill, `-4/-4` overhang, 11pt badge font, Korean accessibility copy
        - PopoverContentView: 11pt button font, 12pt button horizontal padding, 8pt button top padding, "모두 지우기" copy, `.thinMaterial` background
        - PopoverRowView: hover color `Color(NSColor.controlAccentColor).opacity(0.12)`, hover animation `easeInOut(duration: 0.12)`, Phase 3 D3-11 missing-animation timings (`0.15s × 2`, `±12°`, `0.4s` collapse, `0.2s` reduce-motion fallback), typography (`13pt` body, `11pt` time suffix / `?` indicator)
      - F-3: ColorTokens.accent / accentDark / statusSuccess / statusError / statusWaiting tokens are defined for Phase 4 readiness (SC#5) but have ZERO existing call-sites in the current 4-view scope. Their drift-guard tests pass; their consumption begins in Phase 4 (multi-session UX) which will introduce status dots + accent-tinted UI per SPEC.md §3.

    - **Phase 4 Readiness Contract** (SC#5 — explicit token-name list Phase 4 plans must reference):
      ```
      Phase 4 (Multi-Session UX) — when authoring counter-badge widget, expandable session list,
      and per-row status indicators, consume the following symbols from App/DesignTokens.swift
      WITHOUT adding new color/geometry/motion literals:

        ColorTokens.accent           — primary brand color, success status fill (SPEC §3 #D97757)
        ColorTokens.accentDark       — pressed/active accent variant (SPEC §3 #B8492C)
        ColorTokens.statusSuccess    — success status dot fill (SPEC §3 #D97757)
        ColorTokens.statusError      — error status dot fill (SPEC §3 #E5484D)
        ColorTokens.statusWaiting    — waiting status dot fill (SPEC §3 #F5A623)
        GeometryTokens.popoverWidth  — popover container width (280, F-1 code SoT)
        GeometryTokens.popoverCornerRadius
        GeometryTokens.rowMinHeight
        GeometryTokens.rowHorizontalPadding
        GeometryTokens.rowVerticalPadding
        GeometryTokens.popoverMaxVisibleRows
        MotionTokens.bounceDuration
        MotionTokens.bounceOffset
        MotionTokens.bounceAnimation(reduceMotion:)

      If Phase 4 needs a token NOT in this list (e.g., status-dot diameter — SPEC §3 says 7pt
      with hollow ring stroke 1.5pt), add it to App/DesignTokens.swift in the Phase 4 plan that
      first consumes it, with a SPEC.md §3 or §4 citation comment. Drift-guard test in
      DesignTokensTests must be added in the same commit.
      ```

    - Open follow-ups: F-1 SPEC.md reconciliation (v1 patch or v2 visual update); F-3 first-consumer is Phase 4 status dots.

    **C. Commit:**
    ```sh
    git add .planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md \
            .planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md \
            .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md
    git commit -m "docs(03.1): close Phase 03.1 — phase_gate green (design-system foundation)"
    ```

    Update STATE.md `Last updated` line + Current Position + ROADMAP.md Phase 03.1 status (`- [x]`) in a follow-up commit (this is normal phase-close hygiene, not a fresh decision).
  </action>
  <verify>
    <automated>set -e; cd /Users/choijihye/Study/source/claude_alert_bot &amp;&amp; test -f .planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md &amp;&amp; test -f .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md &amp;&amp; grep -c 'phase_gate: green' .planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md &amp;&amp; grep -c 'SC#1' .planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md &amp;&amp; grep -c 'SC#5' .planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md &amp;&amp; grep -c 'F-1' .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md &amp;&amp; grep -c 'F-2' .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md &amp;&amp; grep -c 'F-3' .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md &amp;&amp; grep -c 'Phase 4 Readiness Contract' .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md &amp;&amp; grep -c 'ColorTokens.accent' .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md &amp;&amp; grep -c 'GeometryTokens.popoverWidth' .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md &amp;&amp; grep -c 'MotionTokens.bounceAnimation' .planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md</automated>
  </verify>
  <done>
    - 03.1-VERIFICATION.md exists with `phase_gate: green` (gated by Task 2 approval)
    - 03.1-VERIFICATION.md has explicit verdict blocks for SC#1..SC#5
    - 03.1-SUMMARY.md exists with F-1, F-2, F-3 findings sections
    - 03.1-SUMMARY.md "Phase 4 Readiness Contract" block lists every Phase-4-consumable token symbol
    - Commit landed on master with message `docs(03.1): close Phase 03.1 — phase_gate green (design-system foundation)`
  </done>
</task>

</tasks>

<verification>
1. Phase 2 verifier still green (PASS/FAIL/SKIP profile matches Phase 2 closure baseline).
2. Full XCTest target green; total count ≥ 92 (was 79 + 13 new DesignTokensTests).
3. Manual checkpoint approved by human — every W-1..W-3 / P-1..P-5 slot annotated `pass` or with a regression note.
4. 03.1-VERIFICATION.md exists with `phase_gate: green` and SC#1..SC#5 verdicts.
5. 03.1-SUMMARY.md "Phase 4 Readiness Contract" block enumerates the 14 token symbols Phase 4 plans must consume by name.
6. STATE.md + ROADMAP.md updated to reflect Phase 03.1 closure (in a follow-up commit, normal hygiene).
</verification>

<success_criteria>
- All 5 success criteria from ROADMAP.md Phase 03.1 verified and signed off.
- Phase 4 unblocked — its plans can reference the tokens by name.
- F-1 (popover width 280 vs SPEC's 270) recorded as v1 follow-up; not blocking.
</success_criteria>

<output>
Final phase artifacts:
- `.planning/phases/03.1-design-system-foundation/03.1-MANUAL-CHECKPOINT.md` — visual fidelity checklist with W-1..W-3, P-1..P-5 verdicts
- `.planning/phases/03.1-design-system-foundation/03.1-VERIFICATION.md` — phase_gate: green sign-off + SC#1..5 verdicts
- `.planning/phases/03.1-design-system-foundation/03.1-SUMMARY.md` — consolidated findings (F-1/F-2/F-3) + Phase 4 readiness contract
- `.planning/phases/03.1-design-system-foundation/03.1-05-SUMMARY.md` — this plan's per-plan summary (created by execute-phase boilerplate after Task 3)
</output>
