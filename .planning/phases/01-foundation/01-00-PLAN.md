---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/verify-phase-1.sh
autonomous: true
requirements: [HOOK-01, HOOK-03, HOOK-04, HOOK-05, HOOK-06, IPC-01, IPC-02, IPC-03, DIST-01, DIST-05]
tags: [macos, shell, validation-harness]
must_haves:
  truths:
    - "scripts/verify-phase-1.sh exists, is executable, and runs to completion (with no Wave 0 dependencies satisfied yet, individual checks may fail; the harness itself must not error out)"
    - "Harness supports both --quick (file existence + codesign + LSUIElement) and full mode (build + launch + hook fire + OSLog grep + single-instance)"
    - "Every row in 01-VALIDATION.md `## Per-Task Verification Map` (1-00-01 through 1-07-01) maps to a discrete check function in the harness"
  artifacts:
    - path: scripts/verify-phase-1.sh
      provides: "Single-shot validation harness for Phase 1"
      contains: "verify_1_00_01, verify_1_01_01, verify_1_02_01, verify_1_03_01, verify_1_04_01, verify_1_05_01"
  key_links:
    - from: scripts/verify-phase-1.sh
      to: build/Release/ClaudeAlertBot.app (Wave 3 artifact)
      via: codesign -dv + log show
      pattern: "Signature=adhoc"
    - from: scripts/verify-phase-1.sh
      to: $HOME/Library/Application Support/ClaudeAlertBot/sock (Wave 2 runtime artifact)
      via: test -S
      pattern: "test -S"
---

<objective>
Wave 0 검증 하니스(harness) 스캐폴딩. Phase 1의 모든 후속 wave가 자동화된 트립와이어(tripwire)에 대해 commit하도록 레일을 깐다. 이 하니스 자체는 어떤 production 코드도 만들지 않는다 — 다른 plan들이 만들 산출물을 검증하는 shell 스크립트일 뿐이다.

Purpose: 01-VALIDATION.md의 14행짜리 Per-Task Verification Map을 단일 실행 가능한 shell 스크립트로 구현하여, 매 task commit 후 `--quick` 모드로 < 5초 sampling, 매 wave merge 후 full 모드로 < 20초 sampling이 가능하게 한다.
Output: `scripts/verify-phase-1.sh` 하나의 실행 가능한 스크립트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-phase-1.sh 검증 하니스 작성</name>
  <files>scripts/verify-phase-1.sh</files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map — 14 rows are the ground truth this script must implement)
    - .planning/phases/01-foundation/01-RESEARCH.md "Validation Architecture" section (Phase 1 Success Criteria → Verification Recipe)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07: OSLog subsystem `com.claudealert.bot.hook`, D-10: socket path `~/Library/Application Support/ClaudeAlertBot/sock`)
  </read_first>
  <action>
    Create `scripts/verify-phase-1.sh` as a POSIX-compliant `#!/bin/bash` script with `set -uo pipefail` (NOT `-e` — we want all checks to run even on first failure, then aggregate). Structure:

    1. Header: shebang `#!/bin/bash`, `set -uo pipefail`, color codes (`RED='\033[0;31m'`, `GREEN='\033[0;32m'`, `RESET='\033[0m'`).
    2. Constants:
       ```
       APP_PATH="${APP_PATH:-build/export/ClaudeAlertBot.app}"
       SOCK="$HOME/Library/Application Support/ClaudeAlertBot/sock"
       LOG_FILE="$HOME/Library/Logs/ClaudeAlertBot/hook.log"
       LOG_SUBSYS="com.claudealert.bot.hook"
       ```
    3. CLI flag parser: support `--quick` (only check file existence + codesign + LSUIElement, skip launch/IPC tests) and bare invocation (full suite).
    4. Aggregation: maintain `PASS=0`, `FAIL=0`, `RESULTS=()` arrays. Each check function `verify_X_YY_ZZ()` prints either `[PASS] 1-XX-YY: <name>` or `[FAIL] 1-XX-YY: <name>: <reason>` and increments counters.
    5. Implement these 14 check functions (one per VALIDATION row), each callable independently. Ground truth — copy the exact commands from 01-VALIDATION.md verbatim:

       - `verify_1_00_01`: `bash scripts/build.sh && test -d "$APP_PATH"` (Wave 3 dep — skip in --quick)
       - `verify_1_00_02`: `codesign -dv --verbose=4 "$APP_PATH" 2>&1 | grep -q 'Signature=adhoc'` (Wave 3 dep)
       - `verify_1_01_01`: `test -f ClaudeAlertBot.xcodeproj/project.pbxproj && grep -q 'cab-test' ClaudeAlertBot.xcodeproj/project.pbxproj`
       - `verify_1_01_02`: `/usr/libexec/PlistBuddy -c "Print :LSUIElement" App/Info.plist | grep -q true`
       - `verify_1_02_01` (IPC): `pgrep -f ClaudeAlertBot >/dev/null && test -S "$SOCK"` (full only)
       - `verify_1_02_02`: `log show --last 30s --predicate 'subsystem == "com.claudealert.bot.hook"' 2>&1 | grep -q "listener bound"` (full only)
       - `verify_1_03_01` (Reporter JSON valid): `printf '{"session_id":"x","cwd":"/tmp"}' | bash Reporter/cab-report.sh; tail -1 "$LOG_FILE" | python3 -c "import sys,json; obj=json.loads(sys.stdin.read()); assert 'envelope' in obj"`
       - `verify_1_03_02` (Reporter exit 0 with no socket): `mv "$SOCK" "$SOCK.bak" 2>/dev/null; printf '{}' | bash Reporter/cab-report.sh; rc=$?; mv "$SOCK.bak" "$SOCK" 2>/dev/null; test "$rc" -eq 0`
       - `verify_1_03_03` (Reporter ≤ 50ms): use `/usr/bin/time -p` or `date +%s%N` deltas, assert real ≤ 0.050s with socket absent
       - `verify_1_03_04` (hook.log accumulates): `test -f "$LOG_FILE" && [ "$(wc -l <"$LOG_FILE")" -ge 1 ]`
       - `verify_1_04_01` (cab-test → OSLog): `"$APP_PATH/Contents/MacOS/cab-test" --synthetic && sleep 1 && log show --last 5s --predicate 'subsystem == "com.claudealert.bot.hook"' 2>&1 | grep -q session_id` (full only)
       - `verify_1_05_01` (single-instance): `open "$APP_PATH"; sleep 1; open "$APP_PATH"; sleep 1; [ "$(pgrep -fc ClaudeAlertBot)" -eq 1 ]` (full only)
       - `verify_1_06_01` (manual): print "MANUAL CHECK REQUIRED — see 01-VALIDATION.md Manual-Only Verifications row 1" and skip (does NOT increment FAIL)
       - `verify_1_07_01`: `test -x scripts/verify-phase-1.sh` (self-check)

    6. Main dispatcher:
       ```
       if [[ "${1:-}" == "--quick" ]]; then
           verify_1_01_01; verify_1_01_02; verify_1_03_04; verify_1_07_01
       else
           # full suite — call all 14 in dependency order
       fi
       echo
       echo "Results: ${GREEN}$PASS pass${RESET}, ${RED}$FAIL fail${RESET}"
       exit $(( FAIL > 0 ? 1 : 0 ))
       ```
    7. Make executable: ensure final task action runs `chmod +x scripts/verify-phase-1.sh`.

    Important — Wave 0 reality: when this script runs immediately after creation (before Plans 01-06 land), most checks will FAIL. That is expected. The acceptance criterion below only requires the script itself runs without bash-syntax errors and produces the aggregated PASS/FAIL summary. Individual check failures are by design until later waves complete.

    Per CLAUDE.md "No Over-Editing": this is a brand-new file, no existing code to preserve. Keep functions small and tightly scoped — do NOT inline build.sh logic, do NOT spawn the app from Wave 0 (use `pgrep` to detect).
  </action>
  <verify>
    <automated>bash -n scripts/verify-phase-1.sh && test -x scripts/verify-phase-1.sh && bash scripts/verify-phase-1.sh --quick; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f scripts/verify-phase-1.sh`
    - Executable: `test -x scripts/verify-phase-1.sh`
    - Bash syntax valid: `bash -n scripts/verify-phase-1.sh` exits 0
    - Contains all 14 verify functions: `grep -cE '^verify_1_(0[0-7])_(0[1-9])\(\)' scripts/verify-phase-1.sh` returns ≥ 14
    - Defines APP_PATH, SOCK, LOG_FILE, LOG_SUBSYS constants: `grep -E '^(APP_PATH|SOCK|LOG_FILE|LOG_SUBSYS)=' scripts/verify-phase-1.sh | wc -l` ≥ 4
    - Supports --quick flag: `grep -q 'quick' scripts/verify-phase-1.sh`
    - Self-check passes when invoked: `bash scripts/verify-phase-1.sh --quick` produces output containing "Results:" and exits with 0 or 1 (not crash)
  </acceptance_criteria>
  <done>scripts/verify-phase-1.sh exists, is executable, passes `bash -n`, contains all 14 verify_X_YY_ZZ functions matching 01-VALIDATION.md row IDs, and `bash scripts/verify-phase-1.sh --quick` runs to a "Results: N pass, M fail" summary line without bash errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-shell → harness script | Harness must not assume root; runs as the developer's user |
| harness → user-data paths (~/Library/...) | Read-only inspection; never mutate beyond restoring backup files (1-03-02 swap-back) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-VRFY-01 | Tampering | verify_1_03_02 mv/restore of $SOCK | mitigate | Always restore via `trap "mv \"$SOCK.bak\" \"$SOCK\" 2>/dev/null" EXIT` so a Ctrl-C mid-test does not orphan the user's real socket file |
| T-VRFY-02 | DoS | `open "$APP_PATH"` in single-instance test | mitigate | Test wraps with `pgrep -fx ClaudeAlertBot | xargs -r kill` cleanup on EXIT to avoid leaving stray processes after a failed run |
| T-VRFY-03 | Information Disclosure | grep'ing OSLog by predicate may surface user paths | accept | This is the developer's own log on their own machine; not exfiltrated |
</threat_model>

<verification>
Wave 0 completion:
- `bash -n scripts/verify-phase-1.sh` exits 0 (syntax valid)
- `bash scripts/verify-phase-1.sh --quick` runs to completion and prints "Results:" summary
- Re-running the harness after Wave 1+ completes shows PASS counts increasing as later plans land
</verification>

<success_criteria>
- scripts/verify-phase-1.sh in place, executable, syntactically valid
- Implements all 14 check functions matching 01-VALIDATION.md rows verbatim
- `--quick` and full modes both supported
- Acts as the canonical Phase 1 sampling tripwire for all later waves
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md` documenting: which validation rows are now wired, what's still red (everything except 1-07-01), what depends on this harness in later waves.
</output>
