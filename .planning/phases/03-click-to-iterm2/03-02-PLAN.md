---
phase: 03-click-to-iterm2
plan: 02
type: execute
wave: 1
depends_on: [03-00]
files_modified:
  - Reporter/cab-report.sh
  - App/HookEvent.swift
  - CabTest/main.swift
autonomous: true
requirements: [JUMP-02]
tags: [phase-3, wave-1, d-adapter, d3-05, envelope]
must_haves:
  truths:
    - "Reporter cab-report.sh captures $TERM_PROGRAM and emits it in the envelope as term_program (or null when env var unset)."
    - "HookEvent decodes envelopes with AND without term_program field; missing key yields nil via Swift's Decodable optional default."
    - "CabTest/main.swift payload includes term_program populated from ProcessInfo.processInfo.environment[\"TERM_PROGRAM\"] (mirroring the existing ITERM_SESSION_ID pattern)."
    - "Existing Phase 1 + Phase 2 tests all pass — adding optional fields is forward+backward compatible (D-08 schema_version stays 1)."
  artifacts:
    - path: Reporter/cab-report.sh
      provides: "TERM_PROGRAM env capture + python env injection + JSON envelope key (3 sites parallel to existing ITERM)"
      contains: "TERM_PROGRAM"
    - path: App/HookEvent.swift
      provides: "term_program: String? optional Codable field (schema_version=1 preserved)"
      contains: "term_program"
    - path: CabTest/main.swift
      provides: "Payload mirror of new envelope field for verifier-driven testing"
      contains: "TERM_PROGRAM"
  key_links:
    - from: Reporter/cab-report.sh
      to: App/HookEvent.swift
      via: "AF_UNIX socket envelope JSON term_program field"
      pattern: "term_program"
---

<objective>
Wave 1 envelope extension. Plant the D-ADAPTER `term_program` envelope field through all three sites that the existing `iterm_session_id` triple touches (Reporter env capture → python injection → JSON key) and the App-side decoder. v1 does NOT use this field for dispatch (lock per CONTEXT D-ADAPTER) — it is the v2 multi-terminal branch key (MTERM-01..04). Adding it now means v2 needs zero hook-pipeline change.

Purpose: D-ADAPTER seam completion at the *envelope* layer. The protocol seam (TerminalJumper) lands in 03-01; the data-flow seam lands here. Because schema_version stays at 1 (optional field addition is forward+backward compatible per D-08), there is no migration cost — old envelopes decode as `term_program: nil`, new envelopes carry the value.

Output: 3-line Reporter shell change, 1-line HookEvent struct change, 1-line CabTest payload mirror. All Phase 1+2 tests stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-click-to-iterm2/03-CONTEXT.md
@.planning/phases/03-click-to-iterm2/03-RESEARCH.md
@.planning/phases/03-click-to-iterm2/03-PATTERNS.md
@.planning/phases/03-click-to-iterm2/03-PLAN-INDEX.md

@Reporter/cab-report.sh
@App/HookEvent.swift
@CabTest/main.swift

<interfaces>
Existing 3-site pattern in Reporter/cab-report.sh — Phase 3 mirrors EACH site verbatim:

- Site 1 (line 22): `ITERM_SESSION_ID_VAL="${ITERM_SESSION_ID:-}"` — env capture with default-empty
- Site 2 (lines 34-41): `ITERM="$ITERM_SESSION_ID_VAL" \` — python env injection inside the JSON= block
- Site 3 (line 62): `"iterm_session_id": nz(env("ITERM")),` — python envelope dict key

HookEvent currently has 10 optional fields (App/HookEvent.swift lines 11-18); each declared as `String?` / `Int?`. Swift's Decodable synthesis treats missing JSON keys as `nil` for optional properties — no custom init required.

CabTest payload pattern (CabTest/main.swift line 24): `"iterm_session_id": ProcessInfo.processInfo.environment["ITERM_SESSION_ID"] ?? NSNull()` — Phase 3 adds an exact-shape sibling for TERM_PROGRAM.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Reporter/cab-report.sh — TERM_PROGRAM env capture + python injection + JSON key (3 sites)</name>
  <files>Reporter/cab-report.sh</files>
  <action>
Per CLAUDE.md "no over-editing — strengthen existing code rather than appending parallel duplicates": each Reporter change is a one-line addition next to the existing ITERM_SESSION_ID_VAL site, mirroring its exact form. Do NOT reorganize the surrounding code. Do NOT add a wrapper function.

**Site 1 — env capture (insert immediately below the existing ITERM_SESSION_ID_VAL line, currently line 22):**

```
TERM_PROGRAM_VAL="${TERM_PROGRAM:-}"
```

Default-empty `:-` keeps the script HOOK-03 compliant (always exit 0): unset env var → empty string, never an error.

**Site 2 — python env injection (insert one new line inside the JSON=$(...) block, between the ITERM= line and the trailing CLAUDE_DIR= line, preserving the trailing backslash):**

```
       TERM_PROGRAM="$TERM_PROGRAM_VAL" \
```

Variable name = TERM_PROGRAM so the python `env("TERM_PROGRAM")` lookup matches the conventional shell name. Do NOT rename to TP_VAR or similar — the Phase 1 ITERM/CLAUDE_DIR convention uses concise prefix names but per CLAUDE.md "preserve original code", the existing names ARE the convention. TERM_PROGRAM is a recognizable POSIX-ish env var name and self-documents.

**Site 3 — python envelope dict (insert one line inside the `envelope = { ... }` literal, immediately after the existing iterm_session_id line, currently line 62):**

```
"term_program": nz(env("TERM_PROGRAM")),
```

`nz(...)` is the existing helper at line 54 (`def nz(v): return v if v else None`) — empty string → None → JSON null. No new helper needed.

**Why 3 sites, not 1:** Pitfall #3 lock — the only safe envelope-construction path is python with env-var injection. Drift in any one site = silent missing field. The verifier in 03-09 will assert the field is present in a real envelope.

**Idempotency check:** TERM_PROGRAM is unset on systems without iTerm2 (e.g. plain ssh) → TERM_PROGRAM_VAL="" → nz() returns None → JSON `"term_program": null` → HookEvent decodes as nil. Backward-compatible.

After the edits, run the existing Phase 1 verifier to confirm no regression:

```
bash scripts/verify-phase-1.sh
```

All previously-passing rows must still pass.
  </action>
  <verify>
    <automated>bash -n Reporter/cab-report.sh &amp;&amp; test "$(grep -c TERM_PROGRAM Reporter/cab-report.sh)" -ge 3</automated>
  </verify>
  <done>Reporter/cab-report.sh syntax is valid. `grep -c TERM_PROGRAM Reporter/cab-report.sh` returns 3 or 4 (3 site occurrences plus possibly one occurrence in the comment header from D3-05 reference). Phase 1 verifier still green.</done>
</task>

<task type="auto">
  <name>Task 2: Add term_program field to HookEvent (one-line struct addition)</name>
  <files>App/HookEvent.swift</files>
  <action>
Per CLAUDE.md "fix only the offending line/block — do not rewrite the whole function": single-line addition to the existing HookEvent struct.

Current struct (App/HookEvent.swift lines 8-19) has 10 optional fields. Add ONE line immediately after the `ts` field, with a trailing comment citing the decision:

```
    let term_program: String?              // D3-05 — $TERM_PROGRAM capture; v1 unused, v2 dispatch key (MTERM-01..04).
```

**Why no custom init(from:) is needed** — Swift's auto-synthesized Decodable for a struct with String?-typed properties accepts both "key present" and "key absent" cases as nil. Existing tests that decode old envelopes (no term_program key) will see term_program == nil. New envelopes (with the key) will carry the value. RESEARCH Pattern 8 confirms this against Apple Decodable docs.

**Why the comment exists** — auditor visibility: the next person reading HookEvent will see WHY the field exists without scrolling through CONTEXT.md. Mirrors the existing per-line comment pattern (line 10 cites event as "stop" | "user_prompt_submit", line 4 cites RESEARCH Pitfall #11).

**No other change.** No new init, no Equatable, no custom CodingKeys (the field name term_program matches the JSON key exactly so default coding keys work).

Run the existing test suite to confirm:

```
xcodebuild test -scheme ClaudeAlertBot -destination 'platform=macOS'
```

All Phase 1+2 tests pass — confirms backward compatibility (Phase 2 fixtures decode unchanged).
  </action>
  <verify>
    <automated>xcodebuild test -scheme ClaudeAlertBot -destination 'platform=macOS' 2>&amp;1 | tail -3 | grep -E 'TEST SUCCEEDED'</automated>
  </verify>
  <done>`grep -c term_program App/HookEvent.swift` returns 1 (the new field declaration; the comment is on the same line). Full xcodebuild test is green. `git diff App/HookEvent.swift` shows ONLY one line added (no whitespace reformatting, no comment churn).</done>
</task>

<task type="auto">
  <name>Task 3: Mirror term_program in CabTest/main.swift payload</name>
  <files>CabTest/main.swift</files>
  <action>
Per CLAUDE.md "fix only the offending line": single-line addition to the existing payload dictionary literal. Do NOT change the parseEventArg helper or the socket-send code.

Current payload (CabTest/main.swift line 24) reads:

```
"iterm_session_id": ProcessInfo.processInfo.environment["ITERM_SESSION_ID"] ?? NSNull(),
```

Add ONE line immediately after the iterm_session_id line, mirroring its exact form:

```
"term_program": ProcessInfo.processInfo.environment["TERM_PROGRAM"] ?? NSNull(),
```

**Why this matters for verification:** Plan 03-09 needs to synthesize an envelope from a non-iTerm2 dev shell context (e.g. SSH, plain Terminal.app) where TERM_PROGRAM may differ from "iTerm.app". CabTest is the only synthetic-envelope path that bypasses Reporter; if it doesn't carry the field, the App-side decode test for "term_program present" can't run from cab-test alone. The Reporter path (Task 1) is the production path, but verification needs both.

**Pattern source:** Same one-line mirror that 02-00 Task 2 did for --event= argv parsing — single-line edit, no helper extraction, preserve all surrounding code byte-identical.

Run the test suite + sentinel:

```
xcodebuild test -scheme ClaudeAlertBot -destination 'platform=macOS'
```

All tests still green.
  </action>
  <verify>
    <automated>test "$(grep -c TERM_PROGRAM CabTest/main.swift)" -ge 1 &amp;&amp; xcodebuild build -scheme ClaudeAlertBot -destination 'platform=macOS' 2>&amp;1 | tail -3 | grep -E 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>`grep -c TERM_PROGRAM CabTest/main.swift` returns 1. xcodebuild build is green. cab-test executable still functional (the change is data-only — doesn't touch the socket-send code).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell env vars (TERM_PROGRAM) → Reporter envelope JSON | Untrusted-ish (env-var content) crosses into App-side struct decode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-IPC-01 (carry) | Tampering | cab-report.sh → HookListener envelope | accept (carry-over) | Phase 1 D-09 schema_version guard + 0600 socket perms already enforce. New optional field does not change the trust surface. |
| T-ENV-01 | Tampering | TERM_PROGRAM env var injected into JSON via python | accept | env injection uses the same python `env()` helper Phase 1 verified — JSON encoding is via json.dumps which escapes any special chars. No AppleScript or shell evaluation downstream (v1 doesn't read the field; v2 will use it as a switch-case discriminator, also no eval). |
</threat_model>

<verification>
1. `bash -n Reporter/cab-report.sh` exits 0 — shell syntax valid.
2. `grep -c TERM_PROGRAM Reporter/cab-report.sh` returns ≥3 (env capture + python env injection + JSON key).
3. `xcodebuild test -scheme ClaudeAlertBot` is green — all Phase 1+2 tests pass with HookEvent extended.
4. `bash scripts/verify-phase-1.sh` is green (or unchanged from pre-Phase-3 baseline).
5. `git diff App/HookEvent.swift` shows ONLY the one-line field addition.
6. `git diff CabTest/main.swift` shows ONLY the one-line payload addition.
</verification>

<success_criteria>
1. Reporter envelope, when fired with `TERM_PROGRAM=iTerm.app`, contains `"term_program": "iTerm.app"`.
2. Reporter envelope, when fired without TERM_PROGRAM env, contains `"term_program": null`.
3. App's HookEvent decodes both forms (present + absent + null) without error.
4. CabTest binary, run with TERM_PROGRAM env set, produces an envelope including the field.
5. All existing Phase 1+2 tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-click-to-iterm2/03-02-SUMMARY.md` recording:
- 3-site Reporter diff (line numbers + exact additions).
- 1-line HookEvent struct addition with comment text verbatim.
- 1-line CabTest payload mirror.
- Confirmation that schema_version stayed at 1 (D-08 + CONTEXT D-ADAPTER lock).
- Confirmation that all Phase 1+2 verifier rows still PASS.
</output>
