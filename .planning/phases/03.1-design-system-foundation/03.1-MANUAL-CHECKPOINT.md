# 03.1 Manual Checkpoint — Visual Fidelity (SC#4)

**Goal:** Confirm the widget and popover are visually indistinguishable before vs after the design-token refactor.

## Automated pre-checks (Task 1 — completed)

- [x] `scripts/verify-phase-2.sh` executed; full log at `/tmp/03.1-verify-phase-2.log`
  - PASS count: **21**
  - FAIL count: **3** (`2-08-02` expected-red post-Phase-3; `2-11-02` documented cab-test artifact; `2-11-99` Phase 1 regression — environmental, see below)
  - SKIP count: **2** (`2-00-02` cab-test predates `--event=` arg; `2-11-03` SC#3 manual)

  **Diff vs Phase 2 closure baseline (23 PASS / 1 FAIL\* / 2 SKIP):**
  - `2-08-02` regressed from PASS → FAIL post-Phase-3 (annotated **EXPECTED RED** in `verify-phase-2.sh` itself; verify-phase-3.sh row 3-09-02 tolerates exactly this single failure when running Phase 2 verifier as a regression chain — documented in `03-VERIFICATION.md`).
  - `2-11-99` Phase 1 regression FAILED because the Phase 1 sub-verifier checks `Reporter ≤ 250ms` (0.45s elapsed — timing/load environmental) and `OSLog 'listener bound' line in last 30s` (requires the app running with NWListener bound). This matches the V-10 carry-over already documented in `STATE.md`. The xctest target ran 123/123 green from a clean build, confirming code correctness; the failure is purely runtime test-state, not a regression introduced by the token refactor.
  - Effective post-Phase-3 baseline: **22 PASS / 2 FAIL\* / 2 SKIP** (with 2-08-02 added as expected-red). Current run is 21/3/2 — diff of −1 PASS / +1 FAIL is the environmental Phase 1 sub-check.

- [x] Full XCTest target executed; log at `/tmp/03.1-xctest.log`
  - Total test count: **123** (Phase 2 closure 79 + DesignTokensTests 15 + others added through Phase 3 = 123 ≥ 92 expected)
  - All passed: **yes (123/123, 0 failures, 0 unexpected)**

## Manual visual checks (human, side-by-side)

Capture pre-refactor screenshots BEFORE running this plan against your local checkout. The recommended commit pin for "before" is whichever commit precedes Plan 01 (e.g. `a8f9a96` — the SPEC reconciliation commit, last commit before `1c4b9c7` test gate). After Plan 04 lands (current state), capture "after" screenshots.

### Floating widget (WidgetIconView)

- [ ] **W-1: Idle bounce — 30s observation.** Vertical travel ≈ 5pt; cycle ≈ 0.45s up + 0.45s down (eye-balled, not measured). Cadence indistinguishable from before.
  - Before screenshot: `[paste path or attachment]`
  - After screenshot: `[paste path or attachment]`
  - Verdict: __

- [ ] **W-2: Reduce-motion toggle.** System Settings → Accessibility → Display → "Reduce motion" ON: bounce stops within ~1 cycle. OFF: bounce resumes.
  - Behaviour matches pre-refactor: __

- [ ] **W-3: +N badge alignment.** With pendingCount ≥ 2, the red badge sits at top-trailing with the same `-4/-4` overhang and 16pt diameter. (F-2 inline literals retained.)
  - Verdict: __

### Popover (PopoverContentView + PopoverRowView)

- [ ] **P-1: Width.** Popover is 280pt wide (post-refactor token); side-by-side with pre-refactor screenshot, no horizontal shift.
  - Verdict: __

- [ ] **P-2: Row height.** Each row is 36pt min; row count of 8 fills the popover ScrollView region without an inner scrollbar; row count ≥ 9 introduces the scrollbar.
  - Verdict: __

- [ ] **P-3: Row hover.** Hovering a row tints it with `controlAccentColor.opacity(0.12)` (system-blue tint), same as before. (F-2 retained.)
  - Verdict: __

- [ ] **P-4: Phase 3 D3-11 missing animation.** Click a row whose iTerm2 tab no longer exists → 도리도리 ±12° once, then collapse + fade over ~0.4s. Same shape and timing as before.
  - Verdict: __

- [ ] **P-5: "모두 지우기" button.** Visible only when ≥2 rows. Position top-trailing, 11pt secondary-label color. (F-2 retained.)
  - Verdict: __

## Findings to surface (will be consolidated into 03.1-SUMMARY.md by Task 3)

- [ ] F-1 (popoverWidth = 280, not SPEC's 270 — already reconciled to 280 in commit `a8f9a96`) acknowledged
- [ ] F-2 (non-SPEC inline literals retained) acknowledged
- [ ] F-3 (`accent` token has zero call-sites in current code, defined for Phase 4) acknowledged

## Resume signal

Type `approved` to proceed to phase closure (Task 3 will write 03.1-VERIFICATION.md and 03.1-SUMMARY.md).
Or describe any visual regression observed — Task 3 will not run until each [ ] above is checked or annotated.
