---
phase: 03.1-design-system-foundation
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - App/PopoverContentView.swift
autonomous: true
requirements: []
must_haves:
  truths:
    - "PopoverContentView's geometry literals (280pt width, 36pt row height, 8 visible rows) resolve through GeometryTokens (SC#1)"
    - "Popover container layout is visually indistinguishable from before (SC#4)"
  artifacts:
    - path: "App/PopoverContentView.swift"
      provides: "Popover container view consuming GeometryTokens"
      contains: "GeometryTokens.popoverWidth"
  key_links:
    - from: "App/PopoverContentView.swift"
      to: "App/DesignTokens.swift"
      via: "same-module reference"
      pattern: "GeometryTokens"
---

<objective>
Refactor `App/PopoverContentView.swift` to consume `GeometryTokens.popoverWidth` (280), `GeometryTokens.rowMinHeight` (36), and `GeometryTokens.popoverMaxVisibleRows` (8) instead of inline literals on lines 86 and 88. **NO behavioral change**: layout dimensions stay identical (per Finding F-1, the token preserves the current 280pt code value, not SPEC's 270pt).

Per Finding F-2: `12pt` horizontal padding (line 69), `8pt` top padding (line 70), `11pt` button font, "모두 지우기" copy, and the `.thinMaterial` background are NOT in scope for tokenisation in this plan — they either lack a SPEC §3/§4 row binding to this view's layout structure (F-2) or are non-numeric/non-color (copy/material). Annotated as F-2 retained literals.

Purpose: SC#1 + SC#4 for the popover container.
Output: Minimum-diff edit to `PopoverContentView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@SPEC.md
@CLAUDE.md
@App/PopoverContentView.swift
@App/DesignTokens.swift
@.planning/phases/03.1-design-system-foundation/03.1-01-SUMMARY.md
</context>

<interfaces>
<!-- Frozen from Plan 01. -->
From `App/DesignTokens.swift`:
```swift
enum GeometryTokens {
    static let popoverWidth: CGFloat = 280       // F-1: code SoT, not SPEC's 270
    static let rowMinHeight: CGFloat = 36
    static let popoverMaxVisibleRows: Int = 8
    // (other tokens not consumed in this plan)
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace popover geometry literals in PopoverContentView with GeometryTokens</name>
  <files>App/PopoverContentView.swift</files>
  <read_first>
    - App/PopoverContentView.swift (full pass)
    - App/DesignTokens.swift
    - SPEC.md §3 §"Geometry"
    - .planning/phases/03.1-design-system-foundation/03.1-01-SUMMARY.md (F-1 popover-width drift, F-2 retained literals)
  </read_first>
  <action>
    Apply the minimum-diff patch below. Per CLAUDE.md "No Over-Editing": preserve every line not explicitly listed. The PopoverContentRules namespace + ScrollView + ForEach body MUST remain byte-identical.

    **Edit 1 — line 86** (currently `.frame(maxHeight: 36 * 8)   // UI-SPEC: max 8 visible rows`):
    Replace with:
    ```swift
    .frame(maxHeight: GeometryTokens.rowMinHeight * CGFloat(GeometryTokens.popoverMaxVisibleRows))
    ```
    Behaviour preservation: `36 * 8 = 288` matches `GeometryTokens.rowMinHeight * CGFloat(GeometryTokens.popoverMaxVisibleRows) = 36.0 * 8.0 = 288.0`. The `Int * Int` → `Int` (288) is implicitly converted to `CGFloat` by `.frame(maxHeight:)`; the new expression is `CGFloat * CGFloat` directly. Identical layout output.

    **Edit 2 — line 88** (currently `.frame(width: 280)`):
    Replace with:
    ```swift
    .frame(width: GeometryTokens.popoverWidth)
    ```

    **Edit 3 — line 4 header comment** (currently `// UI-SPEC §"Hover Popover" — 280pt fixed width, max 8 visible rows + ScrollView with hidden indicators,`):
    Leave the existing comment as-is for historical traceability (it's prose, not a runtime literal), but add a single new comment line above line 6 (`// PopoverContentRules is the pure-function namespace tested in PopoverContentTests.`):
    ```swift
    // Phase 03.1 — geometry literals consume GeometryTokens (F-1: token = 280, code SoT, not SPEC's 270).
    ```

    **DO NOT** change:
    - `import` block
    - `PopoverContentRules` enum (lines 11-34) — pure logic, no geometry/motion literals
    - `PopoverContentView` struct declaration, properties, `@var` defaults
    - The "모두 지우기" Button + its `.font(.system(size: 11))`, `.foregroundStyle(Color(NSColor.secondaryLabelColor))`, `.padding(.horizontal, 12)`, `.padding(.top, 8)` — these are F-2 retained literals (typography + chrome padding without a SPEC §3 row binding to this specific subview)
    - `ScrollView { VStack { ForEach ... } }` body
    - `.scrollIndicators(.hidden)`, `.background(.thinMaterial)`, `.onHover { ... }`

    Final diff scope: 2 line replacements + 1 added comment line. If git diff shows more than ~5 changed lines, STOP — over-editing.
  </action>
  <verify>
    <automated>set -e; cd /Users/choijihye/Study/source/claude_alert_bot &amp;&amp; grep -c 'GeometryTokens.popoverWidth' App/PopoverContentView.swift &amp;&amp; grep -c 'GeometryTokens.rowMinHeight' App/PopoverContentView.swift &amp;&amp; grep -c 'GeometryTokens.popoverMaxVisibleRows' App/PopoverContentView.swift &amp;&amp; ! grep -E 'frame\(width:\s*280\)' App/PopoverContentView.swift &amp;&amp; ! grep -E 'maxHeight:\s*36\s*\*\s*8' App/PopoverContentView.swift &amp;&amp; grep -c '\.thinMaterial' App/PopoverContentView.swift &amp;&amp; grep -c 'PopoverContentRules' App/PopoverContentView.swift &amp;&amp; xcodebuild build -scheme ClaudeAlertBot -destination 'platform=macOS' 2>&amp;1 | tail -5 | grep -c 'BUILD SUCCEEDED' &amp;&amp; xcodebuild test -scheme ClaudeAlertBot -only-testing:ClaudeAlertBotTests/PopoverContentTests -destination 'platform=macOS' 2>&amp;1 | tail -10 | grep -c 'Test Suite .* passed'</automated>
  </verify>
  <done>
    - `grep -c 'GeometryTokens.popoverWidth' App/PopoverContentView.swift` ≥1.
    - `grep -c 'GeometryTokens.rowMinHeight' App/PopoverContentView.swift` ≥1.
    - `grep -c 'GeometryTokens.popoverMaxVisibleRows' App/PopoverContentView.swift` ≥1.
    - Old runtime literals removed: `grep -E 'frame\(width:\s*280\)' App/PopoverContentView.swift` returns 0; `grep -E 'maxHeight:\s*36\s*\*\s*8' App/PopoverContentView.swift` returns 0.
    - PopoverContentRules untouched: `grep -c 'PopoverContentRules' App/PopoverContentView.swift` ≥4 (declaration + 3 usages — value preserved).
    - Build succeeds; PopoverContentTests still pass (no logic-rule drift).
  </done>
</task>

</tasks>

<verification>
1. Token consumption: `grep -E 'GeometryTokens\.' App/PopoverContentView.swift | wc -l` ≥3.
2. Old literals removed from runtime code: `grep -nE '(frame\(width:\s*280|maxHeight:\s*36\s*\*\s*8)' App/PopoverContentView.swift` returns 0 lines.
3. F-2 retained literals still in place (the "모두 지우기" button block is untouched): `grep -c '모두 지우기' App/PopoverContentView.swift` ≥1.
4. PopoverContentTests passes (the rule-namespace untouched).
5. Build succeeds.
</verification>

<success_criteria>
- Popover width (280pt) and max-rows scrollable area (288pt) resolve through GeometryTokens.
- Visual fidelity preserved (Plan 05 manual checkpoint will verify).
- No drive-by edits.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-design-system-foundation/03.1-03-SUMMARY.md` covering:
- Diff summary (lines 86, 88, plus F-1 annotation comment)
- F-1 confirmation: popoverWidth token = 280 (code SoT)
- Token consumption count (`GeometryTokens.*` reference count)
- F-2 retained literal list for this file: 11pt button font, 12pt horizontal padding, 8pt top padding, "모두 지우기" copy, .thinMaterial background
</output>
